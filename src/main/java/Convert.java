public interface Convert {
    String convert();  // why does this converter return values and iMove doesnt?

    // interface is useful wehre you have multiple classes wehre they share a common action but they are executed differently
}
